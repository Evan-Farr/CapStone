@model IEnumerable<SyncMe.Models.Event>
@{
    ViewBag.Title = "ViewCalendar";
}

<br />
<hr />
<div class="col-md-12">
    <div class="text-success">
        @TempData["Message"]
    </div>
    <div class="text-warning">
        @TempData["ErrorMessage"]
    </div>
</div>
<hr />
<container>
    <div class="col-md-1">
        <center>
            @Html.ActionLink("Add New Event", "Create", "Events", null, new { @class = "btn btn-primary btn-md" })
            <br /><br />
            @Html.ActionLink("My Profile", "PrivateDetails", "Members", new { id = ViewBag.Id }, new { @class = "btn btn-primary btn-md" })
            <br /><br />
            @Html.ActionLink("Contacts", "ViewContacts", "Members", new { id = ViewBag.Id }, new { @class = "btn btn-primary btn-md" })
        </center>
    </div>
    <div class="col-md-11">
        <div id="fullCalModal" class="modal fade">
            <div class="modal-dialog">
                <div class="modal-content">
                    <div class="modal-header">
                        <h4 id="modalTitle" class="modal-title"></h4>
                    </div>
                    <h5 id="modalStart" class="modal-body"></h5>
                    <h5 id="modalEnd" class="modal-body"></h5>
                    <h5 id="modalStartTime" class="modal-body"></h5>
                    <h5 id="modalEndTime" class="modal-body"></h5>
                    <h5 id="modalDetails" class="modal-body"></h5>
                    <h5 id="modalPrivate" class="modal-body"></h5>
                    <div class="modal-footer">
                        @Html.ActionLink("Edit", "Edit", "Events", @*new { id =  },*@ new { @class = "btn btn-default" })
                        @Html.ActionLink("Send Invite", "SendInvite", "Members", @*new { id =  },*@ new { @class = "btn btn-default" })
                        @Html.ActionLink("Get Directions", "GetDirections", "Events", @*new { id =  },*@ new { @class = "btn btn-default" })
                        @Html.ActionLink("Weather Forecast", "GetWeather", "Events", @*new { id =  },*@ new { @class = "btn btn-default" })
                        <button type="button" class="btn btn-default" data-dismiss="modal">Close</button>
                    </div>
                </div>
            </div>
        </div>
        <div id='calendar'></div>
    </div>
</container>

<script type="text/javascript">
    $(document).ready(function () {
        var date = new Date();
        var d = date.getDate();
        var m = date.getMonth();
        var y = date.getFullYear();

        $('#calendar').fullCalendar({
            editable: true,
            year: y,
            month: m,
            date: d,
            header: {
                left: 'title',
                right: 'prev,next today,agendaDay,agendaWeek,month'
            },
            defaultView: 'month',
            minTime: 7,
            maxTime: 22,
            editable: true,
            droppable: true,
            slotMinutes: 15,
            disableResizing: false,
            events: @Html.Raw(Json.Encode(Model)),
            eventClick: function(event, jsEvent, view){
                $('#modalTitle').html(event.title);
                $('#modalStart').html("Start Date: " + event.start.toString("MM/dd/yyyy"));
                $('#modalEnd').html("End Date: " + event.end);
                $('#modalStartTime').html("Start Time: " + event.startTime);
                $('#modalEndTime').html("End Time: " + event.endTime);
                $('#modalDetails').html("Details: " + event.details);
                $('#modalPrivate').html("Private: " + event.isPrivate);
                $('#fullCalModal').modal();
            }
        });
    });
</script>
