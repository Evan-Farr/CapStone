@model SyncMe.Models.Profile

@{ 
    if (ViewBag.Pic == 1)
    {
        if (Model.ProfilePictureId != null)
        {
            WebImage wi = new WebImage(Model.ProfilePictureId);
            wi.Resize(700, 700, true, true);
            wi.Write();
        }
    }
}
